#ifndef MEMORY_H
#define MEMORY_H


/*void* operator new(size_t size)
{
    printf("Allocation with size: %zu\n", size);
    // void* p = ::operator new(size);
    void* p = malloc(size);  // will also work fine

    return p;
}

void operator delete(void* p)
{
    printf("Freed!\n");
    free(p);
}*/

namespace Pathfinder
{



}

#endif  // MEMORY_H
